# Agent Skills

`@ultra-omp/agent-skills` packages reusable OMP engineering workflow skills, lifecycle commands, and personas. It is installed through the same catalog as the runtime extensions:

```sh
omp plugin install @ultra-omp/agent-skills
```

The package remains an ordinary OMP extension. The disposable installer only selects and delegates installation; it does not edit OMP's private state.
